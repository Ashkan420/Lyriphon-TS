// Fallback source rules used when the detected language has no dedicated
// fragment, or when language detection failed. Language-agnostic, meaning-first
// guidance.

export const GENERAL_SOURCE = `SOURCE LANGUAGE — GENERAL:
- Translate the MEANING, tone, and imagery of each line — NOT a word-for-word literal rendering. Rebuild the line so it flows naturally in the target language.
- Render idioms, proverbs, and set expressions by their meaning, not their literal words. Do NOT calque expressions across languages.
- If the source language omits subjects or uses a different word order, infer what is implied and reorder so the line reads naturally.
- Convey register and formality (formal vs. casual, affectionate vs. distant) through tone and word choice rather than inventing words.
- Preserve the emotional intent and figurative imagery; keep metaphors that read naturally and adapt those that would sound awkward if translated literally.`;
